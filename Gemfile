source 'https://rubygems.org'

ruby '3.1.0'

# Core dependencies
gem 'sinatra'
gem 'slack-notifier'
gem 'puma'

# Logging
gem 'logger'

# Security
gem 'dotenv'

# Development and testing
group :development, :test do
  gem 'rspec'
end
